# Changelog

## 2026-08-12

- **캘린더에 시간 표기 추가** — **`migration_018` 실행 필요**
  - 캘린더에 뜨는 세 가지 모두 시각을 달 수 있다 — **일정** · **할 일 마감** · **협의된 날짜**
  - **시각은 전부 선택**이다. 안 고르면 예전처럼 날짜만 쓴다
  - 시각을 넣은 항목이 **이른 순서로 앞에** 서고, 시각 없는 항목은 '하루 종일'로 보고 뒤로 보낸다 — 캘린더 칸·날짜 패널·주간 업무·오늘 일정 전부 같은 규칙
  - 시각칸을 넣은 곳: 오늘 기록의 일정 추가, 캘린더·프로젝트 캘린더의 날짜 패널(추가·수정), 일지 수정 폼의 할 일 마감, 종료 프롬프트의 협의된 날짜
  - 할 일 마감은 **날짜를 비우면 시각도 함께 비워진다** — 날짜 없는 시각은 찍힐 자리가 없다
  - 일지 **기록일에는 시각을 달지 않았다** — 약속이 아니라 '그날 적었다'는 뜻이라

- **종료할 때도 협의된 날짜를 여러 개 남길 수 있게 함** — **`migration_017` 실행 필요**
  - 하루로 끝나지 않는 협의가 있어 **날짜를 여러 개 담을 수 있다**. 날짜를 고르고 `날짜 추가`를 누르면 칩으로 쌓이고, 칩의 `×`로 하나씩 미리 뻐다. 같은 날짜를 두 번 넣으면 하나로 합쳋진다
  - **날짜마다 일정 내용을 따로 적는다** — `8/14 구조검토 회의` / `8/20 착공식` 처럼. 내용은 비워두고 날짜만 남겨도 된다
  - 담은 날짜마다 캘린더에 따로 찍히고, 칸에는 그날 일정 내용이 보인다(안 적었으면 결정사항, 그것도 없으면 기록 본문)
  - 종료된 건이라도 '그날로 협의됨'이 있을 수 있는데, 지금까지는 종료하면 달력에서 사라졌음. 이제 날짜를 넣으면 캘린더에 남음
  - **날짜는 선택** — 안 넣으면 예전처럼 그냥 종료. 종료 버튼 문구가 `그냥 종료` / `날짜 남기고 종료` / `결정사항 남기고 종료` / `결정사항·날짜 남기고 종료`로 바뀌어 뭐가 남는지 보임
  - 넣는 곳은 두 군데 — 종료 누를 때 열리는 결정사항 칸(일지 카드·미처리 모아보기·캘린더), 그리고 수정 폼의 상태=종료
  - **캘린더 반영** — 전체 캘린더엔 현장 색 칩에 `결정` 말머리로, 프로젝트 캘린더엔 초록 칩(범례 `협의된 날짜`)으로 찍힘. 날짜를 누르면 아래 패널에 전문이 펼쳐짐
  - 기록일과 협의된 날짜가 같으면 한 번만 표시 (할 일 마감과 같은 규칙)
  - `next_action_date`(내가 해야 할 날)와는 뜻이 달라 컬럼을 따로 둔다 — `decision_date`
  - 적어둔 날짜를 **지우는 건 수정 폼에서만** 된다. 종료 프롬프트에서 비워두면 기존 값이 살아남는다(결정사항과 같은 방식)

## 2026-08-11

- **“할 일”이 세 가지를 가리키던 것을 정리** — 마이그레이션 없음
  - 일지의 할 일 · 일정 · 체크리스트 항목이 화면에서 전부 `할 일`로 보였고, **`등록된 할 일이 없어요`라는 똑같은 문장이 오늘 일정과 체크리스트 두 곳**에 띄우고 있었음
  - 이제 개념마다 단어가 하나다 — **할 일**은 일지에만, `schedule_items`는 **일정**, 체크리스트는 **항목**
  - 오늘 기록: 카드 제목 `할 일 / 일정` → `오늘 일정`, 입력칸 `할 일을 입력하세요` → `일정을 입력하세요`, 빈 문구 `오늘 일정이 없어요`
  - 체크리스트: `할 일 추가` → `항목 추가`, 입력칸 `할 일 *` → `항목 내용 *`, 목록 제목 `할 일` → `체크리스트`, 폴더 삭제 확인창·안내문구도 함께 수정
  - 기능·데이터는 그대로고 화면 문구만 바뀌었음
- **프로젝트 상세 화면에서 캘린더를 맨 위로 이동** — 마이그레이션 없음
  - 순서가 `캘린더 → 정보 카드 → 진행사항 → 협력업체 → 문서함`으로 바뀜. 프로젝트에 들어가면 면적·계약 정보보다 날짜 흐름이 먼저 보임
  - 프로젝트 이름이 정보 카드에만 있어 아래로 밀리므로, 달력 제목을 `프로젝트명 · 진행 기록`으로 바꿔 맨 위에서도 어느 현장인지 보임

## 2026-08-08

- **프로젝트 상세에 그 프로젝트만의 캘린더 추가** — 마이그레이션 없음
  - 기존의 일지 입력 폼 + DESIGN/BUILD 탭 + 기록 목록을 걷어내고 그 자리에 월 달력을 넣음. 일지 작성은 `오늘 기록`에서 하고, 프로젝트 화면은 흐름을 보고 고치는 곳으로 정리
  - 달력에 **일지 기록(작성일)** · **할 일(마감일)** · **일정** 세 가지가 함께 찍힘. 특히 일지 기록은 전체 캘린더에는 안 나오던 것이라, 이 프로젝트가 언제 뭘 했는지가 날짜 위에서 바로 보임
  - 종류별로 색이 다름 — 기록(남색) / 할 일(주황) / 일정(회색). 한 프로젝트만 보는 달력이라 현장 색은 의미가 없어 종류로 나눔
  - **답변 대기 중인 기록은 앞에 보라색 점**이 붙어 달력에서 바로 구분됨 (칸이 좁아 배지 대신 점)
  - 요약줄에 **`답변 대기 N건` 배지** 상시 표시 — 답변 대기는 기록일 칸에만 찍혀 지난 달로 밀리면 달력에서 사라지므로, 어느 달을 보고 있든 몇 건이 물려 있는지는 보이게 함. 누르면 그 프로젝트로 필터된 전체 목록으로 이동
  - 날짜를 누르면 그날 항목이 아래에 전문으로 펼쳐지고, 거기서 바로 상태 변경·수정·삭제 가능 (전체 목록의 일지 카드와 같은 폼)
  - 펼친 날짜에서 그 프로젝트 일정을 바로 추가/수정/완료/삭제 가능
  - `이전`/`이번 달`/`다음`으로 달 이동. `?month=` URL 파라미터라 뒤로가기·링크 공유가 되고, 이동해도 화면이 위로 튀지 않음
  - 기록이 전부 지난 달에 있어도 길을 잃지 않게 `기록 N건 · 처음 2026-07 ~ 최근 2026-08` 요약과, 빈 달일 때 최근 기록 달로 가는 안내를 표시
- **모바일 상단 메뉴 글자 깨짐 수정** — 탭 6개가 한 줄에 안 들어가 `프로젝 / 트`처럼 링크 안에서 글자가 쪼개지던 문제. 모바일에서 3열 2줄 그리드로 배치하고 링크에 줄바꿈 금지 적용. 로그아웃을 앱 이름 옆으로 올려 헤더 높이도 한 줄 줄임. 데스크톱은 기존 한 줄 그대로
- 체크리스트 탭을 **현장 선택 → 현장별 할 일** 두 단계로 변경
- 첫 화면은 현장 카드 목록 — 공정단계, 남은 건수/완료 건수, 준비/협의중/진행중 상태별 건수를 요약 표시
- 카드를 누르면 `/checklist?project=<id>`로 들어가고, `← 현장 목록`으로 돌아옴. URL에 현장이 담겨 뒤로가기와 링크 공유가 됨
- 현장 카드 목록을 **DESIGN / BUILD로 구분**해 각각 섹션과 현장 수 표시 (`PHASES_BY_TYPE` 기준, `/projects`·`/journal`과 같은 구분)
- 완료 항목 보기 토글은 선택한 현장 안에서만 동작 (숨겨진 완료 건수도 버튼에 표시)
- **체크리스트에 폴더(그룹) 개념 추가** — `migration_016`
  - 현장 아래에 폴더를 만들어 할 일을 묶을 수 있음 (예: `설계변경 보완사항`)
  - 폴더마다 별도 목록 + 할 일 추가 폼. 폴더 안에서 추가하면 그 폴더에 담김
  - 할 일 수정 폼의 폴더 선택으로 다른 폴더로 이동 가능
  - 폴더 이름 변경/삭제 지원. **폴더를 지워도 안의 할 일은 지워지지 않고 '폴더 없음'으로 이동**
  - 기존 할 일은 전부 '폴더 없음'에 남고, 폴더를 하나도 안 만들면 예전과 똑같이 보임
- 체크리스트가 **현장 → 폴더 → 할 일** 3단계 탐색으로 정리됨
  - 현장에 들어가면 폴더 카드 목록(폴더별 남은/완료 건수, 상태별 건수)이 먼저 나오고, 고르면 그 폴더 할 일만 표시
  - 폴더 없는 항목이 있으면 `폴더 없음` 카드가 함께 나옴
  - URL이 `/checklist?project=<id>&group=<id>`라 뒤로가기·링크 공유 가능
  - 폴더 이름 변경/삭제는 폴더에 들어간 화면에 있음
- **주간 업무 화면 추가** (`/weekly`, 상단 메뉴 `주간 업무`) — 마이그레이션 없음
  - 일지의 **내가 할 일 + 답변 대기**와 **일정(schedule_items)**을 **프로젝트별로 묶어** 한 주 단위로 표시
  - 월~일 기준. `지난주` / `이번주` / `다음주` 이동 (`?week=` URL 파라미터라 뒤로가기·링크 공유 가능)
  - 프로젝트 안에서 `밀린 것`(기한 지남) / `이번 주` / `답변 대기` / `날짜 없음` 네 묶음으로 정리
  - 일정은 `일정` 태그로 구분, 완료 처리한 일정은 제외
  - 현장이 없는 일정은 `프로젝트 없는 일정`으로 맨 아래에 모임
  - 밀린 게 있는 프로젝트가 위로, 그다음 마감이 이른 순
  - **캘린더에는 안 보이던 '날짜 없는 할 일'과 '답변 대기'도 여기서는 보임**
  - 밀린 것·답변 대기·날짜 없음은 이번 주를 볼 때만 표시 (다른 주에 같은 더미가 따라오지 않도록)
  - 일지에 적어둔 **결과**를 항목 아래 함께 표시. 특히 `답변 대기`는 상태 색(보라) 박스로 강조 — 어디까지 진행됐는지가 핵심이라
  - 읽기 전용 — 상태 변경·수정은 전체 목록의 미처리 모아보기에서
  - 배지만 눌러 '내가 할 일'로 바꿔 `next_action`이 비어 있는 일지도 기록 본문으로 표시
- 마감 배지 규칙(`지남`/`오늘`/`D-N`)을 `src/lib/date.ts`의 `dueBadge()`로 공용화 — 미처리 모아보기와 주간 업무가 같은 색·문구를 씀
- 일지 미처리 모아보기에 **결과** 표시 추가 — 적어둔 결과가 있으면 항목 아래 회색 줄로 함께 보임
- 일지 미처리 모아보기(`/journal` 상단)에 **`수정`** 추가 — 일지 카드와 같은 수정 폼이 그 자리에서 열림 (카테고리·기록·결과·상태·할 일 전부 수정 가능)
- **오늘 기록에서 지난 날짜 일지 작성 가능** — `지난날 기록하기`를 누르면 날짜 선택칸이 열리고, 고른 날짜로 저장됨
  - 오늘이 아닌 날짜를 고르면 `2026년 8월 5일 (수) 기록으로 저장됩니다` 배지로 확인시켜 줌
  - `오늘로` 버튼으로 되돌리기. 미래 날짜는 고를 수 없음(최대 오늘)
  - 저장 후에도 고른 날짜가 유지돼 같은 날 여러 건 연속 입력 가능
  - 마이그레이션 없음 (`work_logs.date`에 그대로 저장)
- `/projects` 화면 표기를 `프로젝트(현장)` → **`프로젝트`**로 변경 (상단 메뉴·설명문·빈 목록 문구 포함)
- 프로젝트 카드에 **미처리 일지 건수 요약** 추가 — `일지 내가 할 일 3 답변 대기 2` 형태의 숫자 칩. 미처리가 없으면 표시 안 함
- 현장 목록(`/projects`) 카드의 체크리스트를 **폴더명 + 상태 요약**으로 축약 — 미완료 항목을 하나씩 나열하던 것을 `📁 폴더명 12 / 준비 4 진행중 8` 형태로 변경. 남은 항목이 없는 폴더는 표시하지 않음

## 2026-08-07 (2)

- 대시보드 상단에 **일지 미처리 모아보기** 추가 — 날짜와 상관없이 '내가 할 일'/'답변 대기' 상태의 일지를 전 현장에서 모아 표시
- 할 일에 날짜가 있으면 `N일 지남`(빨강) / `오늘`(빨강) / `D-N`(주황) 배지로 급한 순서를 표시, 마감일 빠른 순 정렬
- 각 항목에서 바로 상태 변경 — 할 일은 `대기`/`완료`, 답변 대기는 `내 할 일`/`완료`
- 그룹당 5건까지만 보이고 나머지는 '더 보기'로 펼침. 미처리가 없으면 섹션 자체를 숨김
- 마이그레이션 없음 (기존 컬럼만 사용)
- 협의 카테고리 세부 항목 맨 앞에 `발주처` 추가
- 전체 건축일지(`/journal`) 목록 상단에도 미처리 모아보기 노출 — 별도 조회 없이 현재 필터 결과에서 뽑으므로 현장/카테고리 필터를 걸면 함께 좁혀짐
- 상태 배지 색을 진하게 하고 테두리 추가 — 내가 할 일(주황)/답변 대기(보라)/종료(초록)가 확실히 구분됨. 입력 폼의 상태 선택 버튼도 같은 색을 씀
- **종료할 때 결정사항을 남길 수 있게 추가** — `migration_014`
- 상태 배지나 모아보기의 '종료'를 누르면 결정사항 입력칸이 열리고, 적으면 `결정사항 남기고 종료` / 비워두면 `그냥 종료` 버튼으로 바뀜
- 수정 폼에서 상태를 '종료'로 고르면 결정사항 칸이 함께 나옴
- 종료된 일지 카드에 결정사항을 초록 박스로 표시
- 상태를 되돌렸다가 다시 종료해도 적어둔 결정사항은 남아 있음 (할 일과 같은 방식)
- 모아보기의 `기록 8/7` 표기를 `8/7 작성`으로 변경 (마감일로 오해되던 문제)
- **캘린더에서 날짜를 누르면 그날 항목이 아래에 전문으로 펼쳐짐** — 칸에서 잘리던 긴 내용을 다 볼 수 있고, 할 일과 함께 원본 기록 내용·작성일·현장 링크도 표시
- 캘린더 항목에 현장 색 왼쪽 강조선 추가 — 배경 색만으로 구분하던 것보다 현장이 확실히 갈림. 일정(현장 없음)은 회색 선
- **캘린더에서 바로 수정 가능** — 펼친 날짜 패널에서 항목을 그 자리에서 편집
  - 할 일: 문구/날짜 수정, `답변 대기로` 전환, `종료`(결정사항 입력 포함). 날짜를 바꾸면 그 날짜로 옮겨가고, 비우면 캘린더에서 빠짐
  - 일정: 완료 체크, 내용/날짜 수정, 삭제
  - 패널 하단에서 그 날짜에 일정 바로 추가
- **일정(schedule_items)에 현장 지정 가능** — `migration_015`. 지정하면 캘린더에서 그 현장 색으로 표시되고 범례에도 들어감. 지정 안 하면 기존처럼 회색
- 일정 추가/수정 폼(캘린더 패널, 대시보드)에 현장 선택 드롭다운 추가, 대시보드 일정 목록에도 현장 배지 표시
- 현장을 삭제해도 일정은 남고 연결만 끊김 (`on delete set null`)
- **버그 수정: Tailwind가 `src/types/`를 스캔하지 않아 상태 배지 색이 전부 적용되지 않던 문제** — `content` 글롭을 `./src/**/*.{ts,tsx}` 하나로 통일. 이 때문에 일지 상태 배지 색 변경도 실제로는 반영되지 않고 있었음
- 체크리스트 상태 배지 색 강화 — `진행중`을 눈에 띄는 인디고로 바꾸고, 네 상태 모두 배경을 진하게 + 테두리 추가
- 체크리스트 담당자 목록에 **'나'** 추가 (담당자 없음 다음, 협력업체 앞). 담당자별로 묶어 볼 때 '나' 그룹이 맨 위로 올라옴 — 마이그레이션 없음(기존 `assignee` 칸 재사용)
- 오늘 기록(대시보드)에서 미처리 모아보기 제거 — 바로 아래 '오늘 작성한 기록'과 겹쳐서. 전체 건축일지(`/journal`) 상단에는 그대로 있음

## 2026-08-07

- 앱 이름을 "업무일지"에서 "ArchiLog(나의 건축일지)"로 변경 — 헤더/로그인·회원가입 화면/브라우저 탭 타이틀
- UI 표기만 변경했으며 내부 패키지명(`work-journal`)과 배포 설정은 그대로 유지
- 오늘 입력 흐름을 **프로젝트 선택 → 기록 → 결과 → 상태**로 개편 — `migration_012`
- 일지에 `결과`(result) 칸 추가 (선택 입력)
- 일지 상태를 진행중/완료에서 **내가 할 일 / 답변 대기 / 종료** 3단계로 변경. '내가 할 일' 선택 시 할 일 내용(`next_action`)을 함께 입력
- 기존 '진행중' 기록은 마이그레이션에서 '내가 할 일'로 이관
- 일지 카드에 결과와 할 일을 함께 표시하고, 상태 배지를 누르면 3단계로 순환
- 상단 탭 '오늘 입력'을 '오늘 기록'으로 변경
- 현장을 고른 뒤 폼에 또 나오던 프로젝트 선택란 제거, 카테고리를 기록보다 위로 이동
- '내가 할 일'에 선택적 날짜 입력 추가 — `migration_013`
- 날짜를 넣은 할 일은 캘린더에 표시되며, 현장별로 다른 색으로 구분 (색상은 현장 id로 결정, 저장하지 않음)
- 캘린더 상단에 해당 월에 할 일이 있는 현장 색상 범례 추가

## 2026-07-30

- 체크리스트 항목 인라인 수정 기능 추가 (내용/담당자/코멘트를 수정 폼으로 편집)
- 체크리스트 항목에 코멘트(`note`) 필드 추가 — `migration_010`
- 프로젝트(현장)에 면적 정보 추가: 대지면적/건축면적/연면적(m²) — `migration_011`
- 대지면적·건축면적·연면적으로 건폐율/용적률을 자동 계산해 프로젝트 상세정보에 표기 (저장하지 않고 매번 계산)
- 현장 목록(`/projects`) 카드에도 면적·건폐율·용적률 요약 표시
- 면적 표기 시 m² 옆에 평수 자동 환산 표기 (`X.XXm² (Y.YY평)`)
